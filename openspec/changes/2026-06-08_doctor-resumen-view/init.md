# Init: Doctor ResumenView Redesign

**Change ID:** doctor-resumen-view
**Created:** 2026-06-08

## Scope
Redesign `ResumenView` — the default briefing view doctors see first. Currently uses an inline KPI card instead of the shared `KpiCard`/`KpiCards` components.

## Goal
Make ResumenView a proper "morning briefing" with consistent KPIs, cleaner layout, and better information hierarchy.
